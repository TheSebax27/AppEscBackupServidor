<Window x:Class="BackupSyncApp.ConexionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conexión de backup" Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nombre (ej: Contabo 1)" Margin="0,0,0,3"/>
        <TextBox Grid.Row="1" x:Name="TxtNombre" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Servidor (IP o dominio)" Margin="0,0,0,3"/>
        <TextBox Grid.Row="3" x:Name="TxtServidor" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Usuario SSH" Margin="0,0,0,3"/>
        <TextBox Grid.Row="5" x:Name="TxtUsuario" Margin="0,0,0,10"/>

        <Grid Grid.Row="6" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Ruta llave privada SSH" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtLlave"/>
            </StackPanel>
            <Button Grid.Column="1" Content="..." Width="30" Margin="5,18,0,0"
                    VerticalAlignment="Top" Click="BtnBuscarLlave_Click"/>
        </Grid>

        <Grid Grid.Row="7" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                <TextBlock Text="Ruta origen (remota)" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtOrigen"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                <TextBlock Text="Ruta destino (local)" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtDestino"/>
            </StackPanel>
        </Grid>

        <CheckBox Grid.Row="8" x:Name="ChkMoverArchivos"
                   Content="Borrar original remoto tras verificar (mover en vez de copiar)"
                   VerticalAlignment="Top" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="90" Margin="0,0,10,0" Click="BtnCancelar_Click"/>
            <Button Content="Guardar" Width="90" Click="BtnGuardar_Click"/>
        </StackPanel>
    </Grid>
</Window>