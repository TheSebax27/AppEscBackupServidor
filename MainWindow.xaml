<Window x:Class="BackupSyncApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup Sync" Height="550" Width="900">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== Columna izquierda: conexiones ===== -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Conexiones" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="+ Agregar" Width="85" Click="BtnAgregar_Click"/>
                <Button Content="Editar" Width="70" Margin="5,0,0,0" Click="BtnEditar_Click"/>
                <Button Content="Eliminar" Width="70" Margin="5,0,0,0" Click="BtnEliminar_Click"/>
            </StackPanel>

            <ListBox Grid.Row="2" x:Name="ListaConexiones">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Nombre}"
                                       VerticalAlignment="Center" FontSize="13"/>
                            <Button Grid.Column="1" Content="Sincronizar" Width="95"
                                    Tag="{Binding}" Click="BtnSincronizar_Click"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- ===== Columna derecha: estado + log en vivo ===== -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="TxtEstadoActual"
                       Text="Selecciona una conexión y dale clic a Sincronizar"
                       FontWeight="Bold" Margin="0,0,0,8"/>

            <ProgressBar Grid.Row="1" x:Name="BarraProgreso" Height="18"
                         Minimum="0" Maximum="100" Margin="0,0,0,8"/>

            <ListBox Grid.Row="2" x:Name="ListaLog" FontFamily="Consolas" FontSize="12">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Foreground" Value="{Binding Color}"/>
                        <Setter Property="Padding" Value="2,1"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Texto}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="3" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="TxtResumen" VerticalAlignment="Center" FontSize="12"/>
                <Button Grid.Column="1" x:Name="BtnCancelar" Content="Cancelar" Width="90"
                        IsEnabled="False" Click="BtnCancelar_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>